# Project todo list

## To do

- [ ] Add copy and paste functionality with `Ctrl+C` and `Ctrl+V`.
- [ ] Remove the scenario-wide flex spot toggle; keep the individual slot toggles.
- [ ] Add exports to a ToolScreen JAR and a Ninjabrain JAR.
- [ ] Add an **Import from another scenario** button on every scenario, next to **New scenario** and **Duplicate**.
- [ ] Add an **Everything in one** button on the export page that exports all available options.
- [ ] Add instructions on the export page explaining how to import the exported files into Prism Launcher.
- [ ] Export all configured scenarios into the appropriate MiniPracticeKit hotbar NBT and MSCR practice scenarios. Map each configured scenario to its corresponding practice scenario (for example, use the **Blinding** configuration for **Nether exit**).

## Later

- [ ] Find an easy way to import the configured settings into a Prism instance, potentially through a Prism import link.
